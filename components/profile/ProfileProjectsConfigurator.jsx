import React from 'react'
import PropTypes from 'prop-types'

function ProfileProjectsConfigurator(props)
{
   return (
      <div>ProfileProjectsConfigurator</div>
   )
}

ProfileProjectsConfigurator.propTypes = {}

export default ProfileProjectsConfigurator
